# Clash Clan Analytics

Локальный проект аналитики клана Clash of Clans с будущей публикацией только подготовленной публичной части как статического сайта.

**Текущий статус:** `workspace bootstrap`.

На этом этапе создана только безопасная структура. Проект не подключён к Clash of Clans API, сборщик и расчёты не реализованы, GitHub Pages не настроен.

Будущие модули охватывают состав клана, пожертвования, обычные КВ, ЛВК, рейды и Игры кланов. Модуль Игр кланов принят на уровне концепции как отдельное направление активности; источник данных ещё исследуется, поэтому первоначально возможен ручной импорт. Реализация модуля не начата.

## Codebase Memory

Для workspace настроен проект Codebase Memory `D-coc` с root `D:/coc`. Безопасные исключения находятся в `D:\coc\.cbmignore`. Подготовлен refresh-скрипт, работающий только с существующим проектом и требующий полностью чистого Git-дерева. Реальный тестовый запуск скрипта ещё не выполнялся и требует отдельного явного разрешения.

Подробности: [docs/codebase_memory.md](docs/codebase_memory.md).

## Структура

- `src/clan_analytics/` — будущий Python-код API, сборщиков, расчётов и экспортов.
- `site/` — будущий статический сайт; `site/data` предназначен только для подготовленных публичных данных.
- `tests/` — будущие автоматические тесты.
- `scripts/` — будущие безопасные служебные команды.
- `docs/` — концепция, архитектура, политика данных, roadmap и журнал решений.
- `D:\coc\data` — постоянные локальные данные, базы, сырьё, ручные вводы и секреты; вне Git.
- `D:\coc\runs` — результаты отдельных запусков; вне Git.
- `D:\coc\obsidian` — человеческие заметки и решения; вне Git.
- `D:\coc\local` — локальные логи, временные файлы и настройки автоматизации; вне Git.

## Основные правила безопасности

- Git root — только `D:\coc\repo`.
- Секреты хранятся только в `D:\coc\data\secrets` и никогда не публикуются.
- API, сеть, создание ключей, commit, push, GitHub Pages и автоматизация требуют отдельных явных разрешений.
- Публичные данные отделяются от внутренних комментариев и служебных статусов.
- Игроки идентифицируются по `player_tag`; определения метрик версионируются.

## Следующий предполагаемый этап

После согласования руководством и отдельного разрешения — этап 1: минимальная проверка доступности официального Clash of Clans API без построения полноценного сборщика.

## Безопасный просмотр

```powershell
Get-ChildItem -LiteralPath D:\coc -Depth 3
git -C D:\coc\repo status
git -C D:\coc\repo remote -v
```
