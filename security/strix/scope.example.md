# Strix scope example

Status: NOT AUTHORIZED

Execution is prohibited until every required approval is recorded.

This file is a placeholder-only template. It does not identify a real target and must not be edited into an executable or committed with credentials.

| Field | Placeholder |
|---|---|
| Owner | `<target owner>` |
| Authorized by | `<authorizer>` |
| Authorization date | `<YYYY-MM-DD>` |
| Purpose | `<specific assessment purpose>` |
| Target type | `<owned local source, loopback app, or owned test environment>` |
| Exact target | `<exact non-production target>` |
| Environment | `<isolated environment description>` |
| Allowed paths/hosts | `<closed allowlist>` |
| Excluded paths/hosts | `<explicit denylist>` |
| Scan mode | `<quick, standard, or deep after approval>` |
| Network access | `<none or exact approved destinations>` |
| LLM provider | `<approved external provider or approved local model>` |
| Data egress approved | `<yes/no and approved data classes>` |
| Docker approved | `<yes/no plus runtime, image and restrictions>` |
| Credentials approved | `<none or approved credential class and storage, never values>` |
| Start time | `<ISO 8601>` |
| Stop time | `<ISO 8601>` |
| Stop conditions | `<specific conditions plus readiness defaults>` |
| Results location | `<approved ignored absolute path outside Git>` |
| Review owner | `<person responsible for findings handling>` |

Required approvals are tracked separately from this template. Filling a field does not grant permission.
